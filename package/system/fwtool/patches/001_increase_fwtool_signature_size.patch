--- a/fwtool.c
+++ b/fwtool.c
@@ -23,7 +23,7 @@
 #include "crc32.h"
 
 #define METADATA_MAXLEN		30 * 1024
-#define SIGNATURE_MAXLEN	1 * 1024
+#define SIGNATURE_MAXLEN	32 * 1024
 
 #define BUFLEN			(METADATA_MAXLEN + SIGNATURE_MAXLEN + 1024)
 
